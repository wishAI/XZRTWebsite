// Generated by CoffeeScript 1.12.2
(function() {
  $(document).ready(function() {
    $('#btnFile').on('click', function() {
      var file, formData, i, len, ref, results;
      ref = $('#iptFiles')[0].files;
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        file = ref[i];
        formData = new FormData();
        formData.append('file', file);
        formData.append('type', $('#fileType')[0].value);
        console.log('Upload Starting!');
        $.ajax({
          type: 'POST',
          url: '/src/doCacheFile',
          data: formData,
          contentType: false,
          processData: false,
          success: function(data) {
            return console.log(data);
          }
        });
        results.push(console.log('Upload finished!'));
      }
      return results;
    });
    return $('#btnText').on('click', function() {
      var formData, text;
      text = $('#iptText').val();
      formData = new FormData();
      formData.append('text', text);
      console.log('Upload Starting!');
      $.ajax({
        type: 'POST',
        url: '/src/doCacheText',
        data: formData,
        contentType: false,
        processData: false,
        success: function(data) {
          return console.log(data);
        }
      });
      return console.log('Upload finished!');
    });
  });

}).call(this);

//# sourceMappingURL=uploadForm.js.map
